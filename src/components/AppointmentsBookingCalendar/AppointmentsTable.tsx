import {TableContainer, Table, TableBody, TableHead} from '@mui/material'

interface Props {

}

function AppointmentsTable(): JSX.Element {
    return (
        <TableContainer>
            <Table>
                <TableHead>

                </TableHead>
                <TableBody>

                </TableBody>
            </Table>
        </TableContainer>
    );
}

export default AppointmentsTable;